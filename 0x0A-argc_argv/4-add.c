#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
 *
 *
 */
int main(int argc, char **argv[])
{
	int num1, num2, result;

	if (argc = {} )
	{
		printf("0\n");
	}
	else if (argc != isdigit)
			printf("Error\n");
			return 1;

			num1 = atoi(argv[1]);
			num2 = atoi(argv[2]);
			result = num1 + num2;
			printf("%d\n", result);
			return (0);



}
